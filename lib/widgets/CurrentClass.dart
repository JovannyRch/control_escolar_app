import 'package:flutter/material.dart';

class CurrentClass extends StatelessWidget {
  const CurrentClass({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Current class"),
    );
  }
}